css-tricks.com 
emmet cheat sheets 
fonts.google.com (for fonts)
mdn web docs
tailwind media queries(tailwindcss.com)

fontawesome.con(for icons),create account->your account-
> your Kits -> click on the project-> copy the ticket -> paste in the html body (at the end where body ends )

CSS Peeper(for images)